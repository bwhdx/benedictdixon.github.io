/* Bundle of portrait-renderers.js, cursor-fx.js, morph-stage.js, site-content.js, portfolio-app.js — generated by tools/bundle.cjs */

/* ── portrait-renderers.js ────────────────────────────────────────── */
// Portrait renderers — each takes a brightness grid (cols×rows of 0-255)
// and renders it using a different visual primitive. Navy on cream throughout.

const PALETTE = {
  ink: '#1c267a',
  // royal navy
  paper: '#d6c585',
  // warm cream
  inkSoft: '#2c3795',
  paperDark: '#b9a86a'
};

// ──────────────────────────────────────────────────────────
// useGrid — fetch + parse the JSON brightness map once
// ──────────────────────────────────────────────────────────
function useGrid(src = 'assets/portrait_grid.json') {
  const [grid, setGrid] = React.useState(null);
  React.useEffect(() => {
    fetch(src).then(r => r.json()).then(setGrid);
  }, [src]);
  return grid;
}

// Helper: brightness → [0,1] cream coverage, with mid-grey clamped
function coverage(v, lo = 70, hi = 230) {
  const t = (v - lo) / (hi - lo);
  return Math.max(0, Math.min(1, t));
}

// ──────────────────────────────────────────────────────────
// A. Candlesticks — Munger-faithful.
// Cream paper bg.  Per column: thin NAVY wick spanning full height.
// Per cell where the face is bright, draw a thicker CREAM body
// covering the wick, simulating an OHLC body sitting on the wick.
// The face emerges where cream bodies cluster.
// ──────────────────────────────────────────────────────────
function CandlestickPortrait({
  grid,
  width,
  height,
  palette = PALETTE,
  density = 1
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    const colW = width / cols;
    const rowH = height / rows;
    const wickW = Math.max(0.8, colW * 0.13);
    // Bodies fully fill the column at max darkness — no thin cream gap left
    // between adjacent saturated cells.
    const bodyW = colW;

    // 1. Full-height navy wicks in every column (continuous candle field)
    ctx.fillStyle = palette.ink;
    for (let c = 0; c < cols; c++) {
      const x = c * colW + (colW - wickW) / 2;
      ctx.fillRect(x, 0, wickW, height);
    }

    // 2. Linear body fill with a slight gamma curve so dark cells snap toward
    //    full width faster (closes the stripe pattern in shadowy areas while
    //    keeping the candle character in mid-tones).
    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        const cov = 1 - coverage(grid.data[r][c], 50, 200);
        if (cov > 0.05) {
          const filled = Math.pow(cov, 0.65);
          const w = wickW + (bodyW - wickW) * filled;
          const x = c * colW + (colW - w) / 2;
          ctx.fillRect(x, r * rowH, w + 0.4, rowH + 0.5); // +0.4 hairline overlap
        }
      }
    }
  }, [grid, width, height, palette, density]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}

// ──────────────────────────────────────────────────────────
// B. Bar Histogram — vertical bars from top + bottom meeting
// at a column-brightness midline.  Looks like trading volume.
// ──────────────────────────────────────────────────────────
function HistogramPortrait({
  grid,
  width,
  height,
  palette = PALETTE
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    const colW = width / cols;
    const rowH = height / rows;
    const gap = Math.max(0.5, colW * 0.18);
    const barW = colW - gap;
    ctx.fillStyle = palette.ink;
    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        const v = grid.data[r][c];
        const dark = 1 - coverage(v, 60, 210);
        if (dark > 0.08) {
          const h = rowH * (0.4 + dark * 0.6);
          const x = c * colW + gap / 2;
          const y = r * rowH + (rowH - h) / 2;
          ctx.fillRect(x, y, barW, h);
        }
      }
    }
  }, [grid, width, height, palette]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}

// ──────────────────────────────────────────────────────────
// C. Blockchain blocks — square tiles in a chain.
// Each tile filled navy with cream gaps; brightness controls
// tile size (smaller in bright face → more cream paper showing).
// ──────────────────────────────────────────────────────────
function BlockchainPortrait({
  grid,
  width,
  height,
  palette = PALETTE
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    // Coarser grid so blocks read as discrete tiles, not lines.
    const N_COLS = Math.floor(cols / 1.7);
    const N_ROWS = Math.floor(rows / 1.7);
    const cw = width / N_COLS;
    const rh = height / N_ROWS;
    const cell = Math.min(cw, rh);
    ctx.fillStyle = palette.ink;
    for (let r = 0; r < N_ROWS; r++) {
      for (let c = 0; c < N_COLS; c++) {
        const r0 = Math.floor(r * rows / N_ROWS);
        const r1 = Math.floor((r + 1) * rows / N_ROWS);
        const c0 = Math.floor(c * cols / N_COLS);
        const c1 = Math.floor((c + 1) * cols / N_COLS);
        let sum = 0,
          n = 0;
        for (let rr = r0; rr < r1; rr++) {
          for (let cc = c0; cc < c1; cc++) {
            sum += grid.data[rr][cc];
            n++;
          }
        }
        const v = sum / n;
        const dark = 1 - coverage(v, 60, 210);
        if (dark < 0.08) continue;
        // Tile size scales with darkness but never fills the cell.
        const tile = cell * (0.18 + dark * 0.62);
        const rr = Math.min(tile * 0.18, tile / 2);
        const x = c * cw + (cw - tile) / 2;
        const y = r * rh + (rh - tile) / 2;
        ctx.beginPath();
        ctx.moveTo(x + rr, y);
        ctx.lineTo(x + tile - rr, y);
        ctx.quadraticCurveTo(x + tile, y, x + tile, y + rr);
        ctx.lineTo(x + tile, y + tile - rr);
        ctx.quadraticCurveTo(x + tile, y + tile, x + tile - rr, y + tile);
        ctx.lineTo(x + rr, y + tile);
        ctx.quadraticCurveTo(x, y + tile, x, y + tile - rr);
        ctx.lineTo(x, y + rr);
        ctx.quadraticCurveTo(x, y, x + rr, y);
        ctx.closePath();
        ctx.fill();
      }
    }
  }, [grid, width, height, palette]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}

// ──────────────────────────────────────────────────────────
// D. ASCII — characters at each cell, density encodes brightness.
// Terminal/cipher feel.
// ──────────────────────────────────────────────────────────
function AsciiPortrait({
  grid,
  width,
  height,
  palette = PALETTE
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    // Coarser cells, larger characters — has to read as TEXT not pixels.
    const N_COLS = Math.floor(cols / 1.6);
    const N_ROWS = Math.floor(rows / 1.9);
    const cw = width / N_COLS;
    const rh = height / N_ROWS;
    const fontSize = Math.min(cw * 1.9, rh * 1.15);
    ctx.font = `${fontSize}px "JetBrains Mono", "Courier New", monospace`;
    ctx.textBaseline = 'middle';
    ctx.textAlign = 'center';
    ctx.fillStyle = palette.ink;

    // Density ramp — denser → lighter. Block chars at the very dense end
    // give us solid shadow coverage; real characters do the mid-tones; spaces
    // at the bright end open up the face highlights.
    const ramp = '█▓▒$£%#&=+:·.  ';
    for (let r = 0; r < N_ROWS; r++) {
      for (let c = 0; c < N_COLS; c++) {
        const r0 = Math.floor(r * rows / N_ROWS);
        const r1 = Math.floor((r + 1) * rows / N_ROWS);
        const c0 = Math.floor(c * cols / N_COLS);
        const c1 = Math.floor((c + 1) * cols / N_COLS);
        let sum = 0,
          n = 0;
        for (let rr = r0; rr < r1; rr++) for (let cc = c0; cc < c1; cc++) {
          sum += grid.data[rr][cc];
          n++;
        }
        const v = sum / n;
        const dark = 1 - coverage(v, 50, 215);
        // Index 0 (densest) at high dark, last (space) at low dark.
        const idx = Math.floor((1 - dark) * (ramp.length - 1));
        const ch = ramp[idx];
        if (ch && ch !== ' ') {
          ctx.fillText(ch, c * cw + cw / 2, r * rh + rh / 2);
        }
      }
    }
  }, [grid, width, height, palette]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}

// ──────────────────────────────────────────────────────────
// E. Horizontal ticker — each row is a horizontal navy line
// of varying segments.  Currency symbols sprinkled.
// ──────────────────────────────────────────────────────────
function TickerPortrait({
  grid,
  width,
  height,
  palette = PALETTE
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    // Use more rows by oversampling
    const N_ROWS = Math.floor(rows * 1.0);
    const rowH = height / N_ROWS;
    const colW = width / cols;
    const lineH = Math.max(1.5, rowH * 0.55);
    ctx.fillStyle = palette.ink;
    for (let r = 0; r < N_ROWS; r++) {
      const gr = Math.min(rows - 1, Math.floor(r * rows / N_ROWS));
      for (let c = 0; c < cols; c++) {
        const v = grid.data[gr][c];
        const dark = 1 - coverage(v, 60, 210);
        if (dark > 0.08) {
          const w = colW * (0.35 + dark * 0.6);
          const x = c * colW + (colW - w) / 2;
          const y = r * rowH + (rowH - lineH) / 2;
          ctx.fillRect(x, y, w, lineH);
        }
      }
    }
  }, [grid, width, height, palette]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}

// ──────────────────────────────────────────────────────────
// F. Pixel dots — circles of varying radius.  Almost halftone.
// ──────────────────────────────────────────────────────────
function DotPortrait({
  grid,
  width,
  height,
  palette = PALETTE
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!grid || !ref.current) return;
    const canvas = ref.current;
    const dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    canvas.style.width = width + 'px';
    canvas.style.height = height + 'px';
    const ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    ctx.fillStyle = palette.paper;
    ctx.fillRect(0, 0, width, height);
    const cols = grid.cols,
      rows = grid.rows;
    const N_COLS = Math.floor(cols / 1.2);
    const N_ROWS = Math.floor(rows / 1.2);
    const cw = width / N_COLS;
    const rh = height / N_ROWS;
    const maxR = Math.min(cw, rh) * 0.6;
    ctx.fillStyle = palette.ink;
    for (let r = 0; r < N_ROWS; r++) {
      for (let c = 0; c < N_COLS; c++) {
        const r0 = Math.floor(r * rows / N_ROWS);
        const r1 = Math.floor((r + 1) * rows / N_ROWS);
        const c0 = Math.floor(c * cols / N_COLS);
        const c1 = Math.floor((c + 1) * cols / N_COLS);
        let sum = 0,
          n = 0;
        for (let rr = r0; rr < r1; rr++) for (let cc = c0; cc < c1; cc++) {
          sum += grid.data[rr][cc];
          n++;
        }
        const v = sum / n;
        const dark = 1 - coverage(v, 60, 210);
        const radius = maxR * dark;
        if (radius > 0.3) {
          ctx.beginPath();
          ctx.arc(c * cw + cw / 2, r * rh + rh / 2, radius, 0, Math.PI * 2);
          ctx.fill();
        }
      }
    }
  }, [grid, width, height, palette]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true"
  });
}
Object.assign(window, {
  CandlestickPortrait,
  HistogramPortrait,
  BlockchainPortrait,
  AsciiPortrait,
  TickerPortrait,
  DotPortrait,
  useGrid,
  PORTRAIT_PALETTE: PALETTE
});

/* ── cursor-fx.js ────────────────────────────────────────── */
// Cursor candle trail — emits tiny vertical "candle wicks" behind the cursor.
// On click, drops a "candle body" briefly.
// Designed to be subtle: small, low opacity, fades quickly.

function CursorTrail({
  enabled = true
}) {
  const wrapRef = React.useRef(null);
  const lastEmit = React.useRef(0);
  React.useEffect(() => {
    if (!enabled) return;
    const wrap = wrapRef.current;
    if (!wrap) return;
    // Skip on touch-only devices and when the user prefers reduced motion.
    if (window.matchMedia('(hover: none)').matches) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    let frameQueued = false;
    let nextPos = {
      x: 0,
      y: 0
    };
    let lastX = -100,
      lastY = -100;
    let lastTime = performance.now();
    function emit(x, y, velocity) {
      const now = performance.now();
      if (now - lastEmit.current < 28) return;
      lastEmit.current = now;
      const el = document.createElement('div');
      el.style.cssText = `
        position: fixed; left: ${x - 0.5}px; top: ${y - 6}px;
        width: 1.2px; height: ${4 + Math.min(velocity * 0.6, 16)}px;
        background: #1c267a;
        opacity: 0.55;
        pointer-events: none;
        z-index: 100;
        transition: opacity 0.9s linear, transform 0.9s cubic-bezier(.2,.7,.3,1);
        mix-blend-mode: multiply;
      `;
      wrap.appendChild(el);
      // Drop down + fade
      requestAnimationFrame(() => {
        el.style.transform = `translateY(${10 + Math.random() * 6}px)`;
        el.style.opacity = '0';
      });
      setTimeout(() => el.remove(), 950);
    }
    function onMove(e) {
      nextPos = {
        x: e.clientX,
        y: e.clientY
      };
      if (!frameQueued) {
        frameQueued = true;
        requestAnimationFrame(() => {
          frameQueued = false;
          const dx = nextPos.x - lastX;
          const dy = nextPos.y - lastY;
          const dist = Math.sqrt(dx * dx + dy * dy);
          const now = performance.now();
          const dt = Math.max(1, now - lastTime);
          const v = dist / dt * 16;
          lastTime = now;
          if (dist > 4) {
            emit(nextPos.x, nextPos.y, v);
          }
          lastX = nextPos.x;
          lastY = nextPos.y;
        });
      }
    }
    function onClick(e) {
      // Larger "body" on click
      const el = document.createElement('div');
      el.style.cssText = `
        position: fixed; left: ${e.clientX - 5}px; top: ${e.clientY - 14}px;
        width: 10px; height: 28px;
        background: #1c267a;
        opacity: 0.85;
        pointer-events: none;
        z-index: 100;
        transition: opacity 0.7s ease-out, transform 0.7s cubic-bezier(.2,.7,.3,1);
        mix-blend-mode: multiply;
      `;
      wrap.appendChild(el);
      requestAnimationFrame(() => {
        el.style.transform = 'translateY(-18px) scaleY(1.4)';
        el.style.opacity = '0';
      });
      setTimeout(() => el.remove(), 750);
    }
    window.addEventListener('mousemove', onMove, {
      passive: true
    });
    window.addEventListener('click', onClick, {
      passive: true
    });
    return () => {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('click', onClick);
    };
  }, [enabled]);
  return /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    style: {
      position: 'fixed',
      inset: 0,
      pointerEvents: 'none',
      zIndex: 100
    }
  });
}
window.CursorTrail = CursorTrail;

/* ── morph-stage.js ────────────────────────────────────────── */
// ─────────────────────────────────────────────────────────
// MorphStage — the scroll-driven centerpiece.
// Particles sit in a fixed pool; their positions interpolate
// between three states:
//   0. Portrait (Benedict's face in candles)
//   1. Career  (a candlestick timeline of every role)
//   2. World   (a constellation map of countries visited)
// Then back to portrait.
// ─────────────────────────────────────────────────────────

const INK = '#1c267a';
const PAPER = '#d6c585';

// Career timeline, 11 roles in chronological order. Each becomes
// a single candle in the timeline state. High/low are visual only.
const CAREER = [{
  yr: '2014',
  co: 'Bath',
  role: 'Computer Science',
  hi: 0.35,
  lo: 0.65,
  w: 0.6
}, {
  yr: '2015',
  co: 'JPMorgan',
  role: 'Patented B2B payments',
  hi: 0.18,
  lo: 0.55,
  w: 0.9
}, {
  yr: '2018',
  co: 'Amex',
  role: 'R&D, customer acquisition',
  hi: 0.30,
  lo: 0.62,
  w: 0.7
}, {
  yr: '2019',
  co: 'MAIA / Fulcrum',
  role: 'Hedge fund, back to front',
  hi: 0.28,
  lo: 0.58,
  w: 0.7
}, {
  yr: '2021',
  co: 'Icon',
  role: 'Real-time payments',
  hi: 0.20,
  lo: 0.52,
  w: 0.8
}, {
  yr: '2022',
  co: 'Nettle',
  role: 'CPTO · payments & loyalty',
  hi: 0.25,
  lo: 0.58,
  w: 0.8
}, {
  yr: '2023',
  co: 'Pound Token',
  role: 'CTO · UK stablecoin',
  hi: 0.15,
  lo: 0.50,
  w: 0.9
}, {
  yr: '2024',
  co: 'Voi Network',
  role: 'Chief Ecosystem Officer · L1',
  hi: 0.08,
  lo: 0.55,
  w: 1.0
}, {
  yr: '2024',
  co: 'Brdge',
  role: 'Head of Blockchain · AI consultancy',
  hi: 0.20,
  lo: 0.50,
  w: 0.7
}, {
  yr: '2025',
  co: 'Kash',
  role: 'COO & CTO · prediction marketplace',
  hi: 0.05,
  lo: 0.55,
  w: 1.1
}, {
  yr: '2025',
  co: 'DD Ventures',
  role: 'Founding Partner · advisory',
  hi: 0.22,
  lo: 0.48,
  w: 0.7
}];

// Three expeditions — drawn as candlestick sequences traversing the canvas.
// Each route is a horizontal run of candle particles; longer real-world
// journeys get more candles + a longer x-range.
const ROUTES = [{
  name: 'London → Mongolia',
  x0: 0.46,
  x1: 0.96,
  y: 0.28,
  n: 540
}, {
  name: 'Cross-India',
  x0: 0.50,
  x1: 0.86,
  y: 0.46,
  n: 340
}, {
  name: 'Sahara → Atlantic',
  x0: 0.54,
  x1: 0.78,
  y: 0.64,
  n: 220
}];
function lerp(a, b, t) {
  return a + (b - a) * t;
}
function smooth(t) {
  return t < 0 ? 0 : t > 1 ? 1 : t * t * (3 - 2 * t);
}
function clamp(v, lo, hi) {
  return Math.max(lo, Math.min(hi, v));
}

// ─────────────────────────────────────────────────────────
// Build particle pool from TWO brightness grids:
//   • portraitGrid drives the face state
//   • mapGrid drives the world map state
// Same weighted-sampling recipe for both — particles cluster where the
// grid is darkest.
// ─────────────────────────────────────────────────────────
function buildParticles(portraitGrid) {
  const particles = [];
  const TARGET = 1100;

  // ── PORTRAIT — column-aligned candles in the right half of canvas ──
  const PX0 = 0.40,
    PX1 = 1.00,
    PY0 = 0.12,
    PY1 = 0.88;
  const NCOL = 84;
  const {
    cols: pCols,
    rows: pRows,
    data: pData
  } = portraitGrid;
  const colDarkness = new Array(NCOL).fill(0);
  const colProfile = [];
  for (let c = 0; c < NCOL; c++) {
    const gridC = Math.floor((c + 0.5) / NCOL * pCols);
    const prof = [];
    let total = 0;
    for (let r = 0; r < pRows; r++) {
      const v = pData[r][gridC];
      const t = (v - 60) / 150;
      const dark = 1 - clamp(t, 0, 1);
      if (dark > 0.08) {
        prof.push({
          yFrac: r / pRows,
          dark
        });
        total += dark;
      }
    }
    colProfile.push(prof);
    colDarkness[c] = total;
  }
  const totalCD = colDarkness.reduce((a, b) => a + b, 0);
  const portraitParticles = [];
  for (let c = 0; c < NCOL; c++) {
    if (colDarkness[c] < 0.5) continue;
    const nPart = Math.round(TARGET * colDarkness[c] / totalCD);
    if (nPart < 1) continue;
    const colX = PX0 + (c + 0.5) / NCOL * (PX1 - PX0);
    const prof = colProfile[c];
    let acc = 0;
    const cdf = prof.map(p => acc += p.dark);
    for (let i = 0; i < nPart; i++) {
      const t = Math.random() * acc;
      let pick = prof[0];
      for (let j = 0; j < prof.length; j++) {
        if (cdf[j] >= t) {
          pick = prof[j];
          break;
        }
      }
      const yJitter = (Math.random() - 0.5) * 0.004;
      const colY = PY0 + pick.yFrac * (PY1 - PY0) + yJitter;
      portraitParticles.push({
        px: colX,
        py: colY,
        pw: 0.0028,
        ph: 0.018 + Math.random() * 0.010
      });
    }
  }
  while (portraitParticles.length < TARGET) {
    portraitParticles.push({
      ...portraitParticles[portraitParticles.length - 1]
    });
  }
  while (portraitParticles.length > TARGET) portraitParticles.pop();
  for (let i = portraitParticles.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [portraitParticles[i], portraitParticles[j]] = [portraitParticles[j], portraitParticles[i]];
  }

  // ── TIMELINE — candles in the right half ──
  const TX0 = 0.42,
    TX1 = 0.98,
    TYTOP = 0.10,
    TYBOT = 0.74;
  const span = CAREER.length;
  const perCandle = Math.floor(TARGET / span);
  const timelinePositions = [];
  for (let i = 0; i < span; i++) {
    const career = CAREER[i];
    const cxNorm = TX0 + (i + 0.5) / span * (TX1 - TX0);
    const candleW = (TX1 - TX0) / span * 0.22 * career.w;
    const yHi = TYTOP + career.hi * (TYBOT - TYTOP);
    const yLo = TYTOP + career.lo * (TYBOT - TYTOP);
    for (let j = 0; j < perCandle; j++) {
      const t = j / Math.max(1, perCandle - 1);
      const y = yHi + t * (yLo - yHi);
      const segH = (yLo - yHi) / perCandle * 1.6;
      const xJitter = (Math.random() - 0.5) * candleW * 0.7;
      timelinePositions.push({
        tx: cxNorm + xJitter,
        ty: y,
        tw: candleW * 0.22,
        th: Math.max(0.006, segH)
      });
    }
  }
  while (timelinePositions.length < TARGET) {
    timelinePositions.push({
      ...timelinePositions[timelinePositions.length - 1]
    });
  }
  while (timelinePositions.length > TARGET) timelinePositions.pop();

  // ── MAP — three expedition routes drawn as candle sequences ──
  // Each route is a horizontal run of candle particles. Length proportional
  // to the journey's real-world span. Slight vertical jitter for organic feel.
  const mapPositions = [];
  for (const route of ROUTES) {
    for (let i = 0; i < route.n; i++) {
      const t = i / Math.max(1, route.n - 1);
      // Even distribution + small horizontal jitter so candles don't sit on
      // a perfect grid
      const xJitter = (Math.random() - 0.5) * 0.004;
      const yJitter = (Math.random() - 0.5) * 0.018;
      // Random candle "body" height — like a real OHLC chart of varying days
      const h = 0.008 + Math.pow(Math.random(), 1.6) * 0.024;
      mapPositions.push({
        mx: route.x0 + t * (route.x1 - route.x0) + xJitter,
        my: route.y + yJitter,
        mw: 0.0028,
        mh: h
      });
    }
  }
  // Pad to TARGET by duplicating along routes
  while (mapPositions.length < TARGET) {
    const src = mapPositions[Math.floor(Math.random() * mapPositions.length)];
    mapPositions.push({
      ...src
    });
  }
  while (mapPositions.length > TARGET) mapPositions.pop();

  // Combine
  for (let i = 0; i < TARGET; i++) {
    particles.push({
      ...portraitParticles[i],
      ...timelinePositions[i],
      ...mapPositions[i],
      stagger: Math.random() * 0.25
    });
  }
  return particles;
}

// ─────────────────────────────────────────────────────────
// Render loop
// ─────────────────────────────────────────────────────────

// Progress map:
//   0.00 - 0.06  hold portrait
//   0.06 - 0.30  morph portrait → timeline
//   0.30 - 0.42  hold timeline
//   0.42 - 0.62  morph timeline → map
//   0.62 - 0.78  hold map
//   0.78 - 0.96  morph map → portrait
//   0.96 - 1.00  hold portrait

function segment(p) {
  // Returns { from: 'P'|'T'|'M', to: same, t: 0..1 }
  if (p < 0.06) return {
    from: 'P',
    to: 'P',
    t: 0
  };
  if (p < 0.30) return {
    from: 'P',
    to: 'T',
    t: (p - 0.06) / 0.24
  };
  if (p < 0.42) return {
    from: 'T',
    to: 'T',
    t: 0
  };
  if (p < 0.62) return {
    from: 'T',
    to: 'M',
    t: (p - 0.42) / 0.20
  };
  if (p < 0.78) return {
    from: 'M',
    to: 'M',
    t: 0
  };
  if (p < 0.96) return {
    from: 'M',
    to: 'P',
    t: (p - 0.78) / 0.18
  };
  return {
    from: 'P',
    to: 'P',
    t: 0
  };
}

// Portrait fit: face area is the RIGHT half on wide canvases; on narrow
// (mobile) canvases the face moves to the TOP half so text can stack below.
const FACE_ASPECT = 100 / 79; // matches beanie_morph_grid (wider than tall)

function getPortraitArea(W, H) {
  if (W >= H) {
    // wide / desktop: face on the right
    return {
      x0: 0.40,
      x1: 1.00,
      y0: 0.12,
      y1: 0.88
    };
  }
  // tall / mobile: face on the top, full width.
  // Portrait band sits lower than the timeline/map band because the
  // beanie/hair candles cluster at the top of the face, making the
  // visual edge denser and more prone to overlapping the chapter label.
  return {
    x0: 0.05,
    x1: 0.95,
    y0: 0.35,
    y1: 0.69
  };
}
function fitPortrait(px, py, W, H) {
  const PA = getPortraitArea(W, H);
  const areaW_px = (PA.x1 - PA.x0) * W;
  const areaH_px = (PA.y1 - PA.y0) * H;
  const areaAspect = areaW_px / areaH_px;
  const lx = (px - 0.40) / 0.60; // original buildParticles px in [0.40, 1.00]
  const ly = (py - 0.12) / 0.76; // original py in [0.12, 0.88]
  let fx = lx,
    fy = ly;
  if (areaAspect > FACE_ASPECT) {
    const faceW_frac = FACE_ASPECT / areaAspect;
    const offset = (1 - faceW_frac) / 2;
    fx = offset + lx * faceW_frac;
  } else {
    const faceH_frac = areaAspect / FACE_ASPECT;
    const offset = (1 - faceH_frac) / 2;
    fy = offset + ly * faceH_frac;
  }
  return {
    x: PA.x0 + fx * (PA.x1 - PA.x0),
    y: PA.y0 + fy * (PA.y1 - PA.y0)
  };
}
function getState(particle, key, W, H) {
  let s;
  if (key === 'P') {
    const fit = fitPortrait(particle.px, particle.py, W, H);
    s = {
      x: fit.x,
      y: fit.y,
      w: particle.pw,
      h: particle.ph
    };
  } else if (key === 'T') {
    s = {
      x: particle.tx,
      y: particle.ty,
      w: particle.tw,
      h: particle.th
    };
  } else {
    s = {
      x: particle.mx,
      y: particle.my,
      w: particle.mw,
      h: particle.mh
    };
  }
  // Mobile: shift right-half particles to upper portion of canvas, leaving the
  // lower portion for the text card. y0 matches the portrait mobile band (0.28)
  // so candles sit below the chapter/meta labels, not behind them.
  if (W < H && key !== 'P') {
    // P is already handled inside fitPortrait
    s = {
      x: (s.x - 0.40) / 0.60,
      // expand [0.40, 1.00] horizontally to [0, 1]
      y: 0.28 + s.y * 0.34,
      // compress vertically to band [0.28, 0.62]
      w: s.w * 0.95,
      h: s.h * 0.55 // shorter candles when squeezed
    };
  }
  return s;
}
function drawFrame(canvas, particles, progress, W, H) {
  const dpr = window.devicePixelRatio || 1;
  if (canvas.width !== W * dpr || canvas.height !== H * dpr) {
    canvas.width = W * dpr;
    canvas.height = H * dpr;
  }
  const ctx = canvas.getContext('2d');
  ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  ctx.clearRect(0, 0, W, H);
  const seg = segment(progress);
  ctx.fillStyle = INK;
  for (let i = 0; i < particles.length; i++) {
    const p = particles[i];
    const stagger = p.stagger;
    const t = clamp((seg.t - stagger) / (1 - stagger), 0, 1);
    const e = smooth(t);
    const a = getState(p, seg.from, W, H);
    const b = getState(p, seg.to, W, H);
    const x = lerp(a.x, b.x, e) * W;
    const y = lerp(a.y, b.y, e) * H;
    const w = lerp(a.w, b.w, e) * W;
    const h = lerp(a.h, b.h, e) * H;
    const motion = Math.hypot(b.x - a.x, b.y - a.y);
    const alpha = 1 - Math.min(0.25, motion * 0.5 * (1 - Math.abs(0.5 - t) * 2));
    ctx.globalAlpha = alpha;
    ctx.fillRect(x - w / 2, y - h / 2, Math.max(0.7, w), Math.max(0.7, h));
  }
  ctx.globalAlpha = 1;
}

// ─────────────────────────────────────────────────────────
// MorphStage component
// ─────────────────────────────────────────────────────────
function MorphStage() {
  const stageRef = React.useRef(null);
  const canvasRef = React.useRef(null);
  const particlesRef = React.useRef(null);
  const progressRef = React.useRef(0);
  const [phase, setPhase] = React.useState('portrait');
  const portraitGrid = useGrid('assets/beanie_morph_grid.json');

  // Build particles when grid arrives, then draw immediately.
  React.useEffect(() => {
    if (!portraitGrid) return;
    particlesRef.current = buildParticles(portraitGrid);
    drawNow();
  }, [portraitGrid]);

  // Draw function — pulls progress + size + particles each time it's called.
  const drawNow = React.useCallback(() => {
    const c = canvasRef.current;
    const parts = particlesRef.current;
    if (!c || !parts) return;
    const rect = c.getBoundingClientRect();
    const W = rect.width,
      H = rect.height;
    if (W <= 0 || H <= 0) return;
    drawFrame(c, parts, progressRef.current, W, H);
  }, []);

  // Scroll: compute progress, update phase, redraw. No RAF — fires on every
  // scroll event (passive listener) and on resize. Bypasses the iframe-hidden
  // RAF throttling that pauses background tabs entirely.
  React.useEffect(() => {
    function onScroll() {
      const stage = stageRef.current;
      if (!stage) return;
      const r = stage.getBoundingClientRect();
      const vh = window.innerHeight;
      const total = r.height - vh;
      const p = clamp(-r.top / total, 0, 1);
      progressRef.current = p;
      const next = p < 0.18 ? 'portrait' : p < 0.36 ? 'toCareer' : p < 0.48 ? 'career' : p < 0.66 ? 'toMap' : p < 0.82 ? 'map' : p < 0.96 ? 'back' : 'final';
      setPhase(prev => prev === next ? prev : next);
      drawNow();
    }
    onScroll();
    window.addEventListener('scroll', onScroll, {
      passive: true
    });
    window.addEventListener('resize', onScroll);
    // Also redraw on visibility change in case the preview was hidden
    // when the user scrolled.
    document.addEventListener('visibilitychange', onScroll);
    return () => {
      window.removeEventListener('scroll', onScroll);
      window.removeEventListener('resize', onScroll);
      document.removeEventListener('visibilitychange', onScroll);
    };
  }, [drawNow]);
  return /*#__PURE__*/React.createElement("section", {
    className: "morph-stage",
    ref: stageRef,
    "data-screen-label": "03 Career Morph"
  }, /*#__PURE__*/React.createElement("div", {
    className: "morph-sticky"
  }, /*#__PURE__*/React.createElement("canvas", {
    ref: canvasRef,
    className: "morph-canvas",
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement(MorphOverlay, {
    phase: phase,
    progress: progressRef
  })));
}

// ─────────────────────────────────────────────────────────
// Text overlay — fades different cards in/out per phase
// ─────────────────────────────────────────────────────────
function MorphOverlay({
  phase
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "morph-overlay"
  }, /*#__PURE__*/React.createElement("div", {
    className: "morph-meta-tl"
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta meta-strong"
  }, "\xA7 03 \xB7 The Almanack, in three acts"), /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "Scroll to advance")), /*#__PURE__*/React.createElement("div", {
    className: "morph-phase"
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta meta-strong"
  }, "CHAPTER"), /*#__PURE__*/React.createElement("div", {
    className: "morph-phase-name"
  }, phase === 'portrait' ? 'Portrait' : phase === 'toCareer' ? 'Portrait → Career' : phase === 'career' ? 'Career' : phase === 'toMap' ? 'Career → World' : phase === 'map' ? 'World' : phase === 'back' ? 'Returning to portrait' : 'Outro')), /*#__PURE__*/React.createElement(PortraitCard, {
    visible: phase === 'portrait'
  }), /*#__PURE__*/React.createElement(CareerCard, {
    visible: phase === 'career' || phase === 'toCareer' || phase === 'toMap',
    fading: phase === 'toMap'
  }), /*#__PURE__*/React.createElement(MapCard, {
    visible: phase === 'map' || phase === 'toMap' || phase === 'back',
    fading: phase === 'back'
  }), /*#__PURE__*/React.createElement(OutroCard, {
    visible: phase === 'final'
  }));
}
function PortraitCard({
  visible
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `morph-card portrait-card ${visible ? 'in' : 'out'}`
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "\xA7 03 \xB7 Introduction"), /*#__PURE__*/React.createElement("h2", {
    className: "heading"
  }, "An ", /*#__PURE__*/React.createElement("em", null, "operator's"), /*#__PURE__*/React.createElement("br", null), "almanack."), /*#__PURE__*/React.createElement("p", {
    className: "lede"
  }, "A decade-plus building companies across fintech, blockchain and AI. Operator, COO, CTO. Deep across product, technology, operations and ecosystems."));
}
function CareerCard({
  visible,
  fading
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `morph-card career-card ${visible ? 'in' : 'out'} ${fading ? 'fading' : ''}`
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "\xA7 03.1 \xB7 Career"), /*#__PURE__*/React.createElement("h2", {
    className: "heading"
  }, "A decade-plus,", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("em", null, "in candles.")), /*#__PURE__*/React.createElement("ol", {
    className: "career-legend"
  }, CAREER.map((c, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, /*#__PURE__*/React.createElement("span", {
    className: "yr"
  }, c.yr), /*#__PURE__*/React.createElement("span", {
    className: "co"
  }, c.co), /*#__PURE__*/React.createElement("span", {
    className: "rl"
  }, c.role)))));
}
function OutroCard({
  visible
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `morph-card portrait-card ${visible ? 'in' : 'out'}`
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "\xA7 03.3 \xB7 Outro"), /*#__PURE__*/React.createElement("h2", {
    className: "heading"
  }, "The only way", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("em", null, "is through.")), /*#__PURE__*/React.createElement("p", {
    className: "lede"
  }, "Pick the audacious route. Trust the process. Get through it."));
}
function MapCard({
  visible,
  fading
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `morph-card map-card ${visible ? 'in' : 'out'} ${fading ? 'fading' : ''}`
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "\xA7 03.2 \xB7 World"), /*#__PURE__*/React.createElement("h2", {
    className: "heading"
  }, "The long way", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("em", null, "round.")), /*#__PURE__*/React.createElement("p", {
    className: "lede"
  }, "Three expeditions. Fifty-plus countries. London to Mongolia, via Iran, by car. India by tuk-tuk. Sahara to Atlantic, across Morocco."), /*#__PURE__*/React.createElement("dl", {
    className: "map-stats"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "50+"), /*#__PURE__*/React.createElement("dd", null, "Countries")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "3"), /*#__PURE__*/React.createElement("dd", null, "Expeditions")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "12,000mi"), /*#__PURE__*/React.createElement("dd", null, "London \u2192 Mongolia, via Iran")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "2,000mi"), /*#__PURE__*/React.createElement("dd", null, "Cross-India, tuk-tuk"))));
}
window.MorphStage = MorphStage;
window.CAREER = CAREER;

/* ── site-content.js ────────────────────────────────────────── */
// Section components for the portfolio site.
// All static markup — JSX is just for componentisation.

// ─────────────────────────────────────────────────────────
// Header (fixed running banner)
// ─────────────────────────────────────────────────────────
function Runner() {
  const [hidden, setHidden] = React.useState(false);
  const [active, setActive] = React.useState('hero');
  const lastY = React.useRef(0);
  React.useEffect(() => {
    function onScroll() {
      const y = window.scrollY;
      setHidden(y > lastY.current && y > 240);
      lastY.current = y;
      // Find current section
      const sections = ['hero', 'about', 'morph', 'projects', 'services', 'writing', 'reading', 'contact'];
      for (let i = sections.length - 1; i >= 0; i--) {
        const el = document.getElementById(sections[i]);
        if (el && el.getBoundingClientRect().top < 100) {
          setActive(sections[i]);
          break;
        }
      }
    }
    window.addEventListener('scroll', onScroll, {
      passive: true
    });
    onScroll();
    return () => window.removeEventListener('scroll', onScroll);
  }, []);
  return /*#__PURE__*/React.createElement("header", {
    className: `runner ${hidden ? 'hidden' : ''}`
  }, /*#__PURE__*/React.createElement("div", {
    className: "brand"
  }, /*#__PURE__*/React.createElement("div", {
    className: "mark"
  }, "B"), /*#__PURE__*/React.createElement("span", null, "Benedict W. H. Dixon \xB7 An Almanack \xB7 MMXXVI")), /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Sections"
  }, [['about', 'About'], ['morph', 'Career'], ['projects', 'Projects'], ['services', 'Services'], ['writing', 'Writing'], ['contact', 'Contact']].map(([id, label]) => /*#__PURE__*/React.createElement("a", {
    key: id,
    href: `#${id}`,
    className: active === id ? 'active' : '',
    "aria-current": active === id ? 'location' : undefined
  }, label))));
}

// ─────────────────────────────────────────────────────────
// Hero — full-bleed candlestick portrait + masthead
// ─────────────────────────────────────────────────────────
function Hero() {
  const grid = useGrid('assets/beanie_hero_16x9_navySides.json');
  const wrapRef = React.useRef(null);
  const [size, setSize] = React.useState({
    w: 0,
    h: 0
  });
  React.useEffect(() => {
    function measure() {
      const el = wrapRef.current;
      if (!el) return;
      const r = el.getBoundingClientRect();
      setSize({
        w: r.width,
        h: r.height
      });
    }
    measure();
    window.addEventListener('resize', measure);
    return () => window.removeEventListener('resize', measure);
  }, []);
  return /*#__PURE__*/React.createElement("section", {
    id: "hero",
    className: "hero-section",
    "data-screen-label": "01 Hero"
  }, /*#__PURE__*/React.createElement("div", {
    className: "hero-portrait",
    ref: wrapRef
  }, grid && size.w > 0 && /*#__PURE__*/React.createElement(CandlestickPortrait, {
    grid: grid,
    width: size.w,
    height: size.h
  })), /*#__PURE__*/React.createElement("div", {
    className: "hero-top-bar"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap hero-top-grid"
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta meta-strong"
  }, "An Almanack for Finance, Operations & Code"), /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "Vol. I \xB7 MMXXVI \xB7 Edition 01"))), /*#__PURE__*/React.createElement("div", {
    className: "hero-band"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "hero-band-top"
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "\xA7 Cover"), /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, "Portrait rendered in 14,560 candles")), /*#__PURE__*/React.createElement("h1", {
    className: "display hero-title"
  }, "Benedict W. H. ", /*#__PURE__*/React.createElement("em", null, "Dixon.")), /*#__PURE__*/React.createElement("div", {
    className: "hero-band-bottom"
  }, /*#__PURE__*/React.createElement("p", {
    className: "hero-tagline-text"
  }, "An operator who ", /*#__PURE__*/React.createElement("em", null, "ships the code and runs the company"), "\u2014 across finance, blockchain & AI."), /*#__PURE__*/React.createElement("a", {
    className: "hero-scroll",
    href: "#about"
  }, /*#__PURE__*/React.createElement("span", {
    className: "meta"
  }, "Read on"), /*#__PURE__*/React.createElement("span", {
    className: "arrow"
  }, "\u2193"))))));
}

// ─────────────────────────────────────────────────────────
// Ticker tape — between sections, a marquee of career stats
// ─────────────────────────────────────────────────────────
function Ticker() {
  const items = ['$150M ECOSYSTEM', 'KASH · COO & INTERIM CTO', '45K USERS', 'JPMORGAN PATENT', '94% COST REDUCTION', '50+ COUNTRIES', 'VOI NETWORK', '12,000mi LONDON → MONGOLIA', 'POUND TOKEN', 'DD VENTURES', 'AMEX R&D', '6 MONTHS SOLO BUILD', 'BATH · COMPUTER SCIENCE'];
  const line = items.join('   ·   ');
  return /*#__PURE__*/React.createElement("div", {
    className: "ticker"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ticker-track"
  }, /*#__PURE__*/React.createElement("span", null, line, "   \xB7   ", line)));
}

// ─────────────────────────────────────────────────────────
// About
// ─────────────────────────────────────────────────────────
function About() {
  return /*#__PURE__*/React.createElement("section", {
    id: "about",
    className: "about-section",
    "data-screen-label": "02 About"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    className: "num"
  }, "\xA7 02 \xB7 About"), /*#__PURE__*/React.createElement("h2", {
    className: "ttl"
  }, /*#__PURE__*/React.createElement("em", null, "From co-inventing"), /*#__PURE__*/React.createElement("br", null), "payment systems to running them.")), /*#__PURE__*/React.createElement("div", {
    className: "right"
  }, "Edition 01 \xB7 pp. 12\u201314")), /*#__PURE__*/React.createElement("div", {
    className: "about-grid"
  }, /*#__PURE__*/React.createElement("div", {
    className: "body about-lede drop"
  }, /*#__PURE__*/React.createElement("p", null, "A decade-plus running and building companies in financial technology. Operator, COO, CTO. Deep across product, technology, operations and ecosystems. The throughline is finance, top to bottom."), /*#__PURE__*/React.createElement("p", null, "JPMorgan to Kash. A patented B2B payment system. A $150M L1 ecosystem with 45K+ users. A stablecoin steered through acquisition. An enterprise-grade microservice platform shipped solo. The breadth is the point \u2014 and every one reached production and moved real money, live, at volume, without incident.")), /*#__PURE__*/React.createElement("aside", {
    className: "margin-note about-margin"
  }, /*#__PURE__*/React.createElement("p", null, "Read also \u2014"), /*#__PURE__*/React.createElement("p", null, "\xA7 03 An operator's almanack", /*#__PURE__*/React.createElement("br", null), "\xA7 04 Featured projects", /*#__PURE__*/React.createElement("br", null), "\xA7 05 Services on offer"), /*#__PURE__*/React.createElement("p", {
    style: {
      marginTop: 16
    }
  }, "Contact \u2014 ", /*#__PURE__*/React.createElement("br", null), "me@benedictdixon.com"))), /*#__PURE__*/React.createElement("hr", {
    className: "hairline",
    style: {
      margin: '64px 0'
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "three-cards"
  }, /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 02.a \xB7 Operational"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Operational ", /*#__PURE__*/React.createElement("em", null, "Leadership")), /*#__PURE__*/React.createElement("p", null, "Companies and divisions, run from the ground up. Organisational structure, financial controls, operational processes. Built to scale. Full-picture ownership.")), /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 02.b \xB7 Product & Technology"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Product & ", /*#__PURE__*/React.createElement("em", null, "Technology")), /*#__PURE__*/React.createElement("p", null, "Distributed systems, event-driven architectures, financial transaction infrastructure. Product strategy, roadmaps and the calls that turn architecture into a business. Designed, built and shipped end-to-end, handling real money in production. AI woven into the products themselves \u2014 and into how they get built.")), /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 02.c \xB7 Commercial"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Growth & ", /*#__PURE__*/React.createElement("em", null, "Go-to-Market")), /*#__PURE__*/React.createElement("p", null, "Engines that take products to market: ecosystem partnerships, community growth, sales strategy and stakeholder management. Zero to 45K+ users and $150M+ in value."))), /*#__PURE__*/React.createElement("hr", {
    className: "hairline",
    style: {
      margin: '64px 0 32px'
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "achievements"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "Key achievements"), /*#__PURE__*/React.createElement("ol", {
    className: "achievement-list"
  }, /*#__PURE__*/React.createElement("li", null, /*#__PURE__*/React.createElement("span", {
    className: "ach-num"
  }, "i."), /*#__PURE__*/React.createElement("span", null, "Built and scaled a blockchain ecosystem from concept to ", /*#__PURE__*/React.createElement("em", null, "$150M+"), " with 45K+ users.")), /*#__PURE__*/React.createElement("li", null, /*#__PURE__*/React.createElement("span", {
    className: "ach-num"
  }, "ii."), /*#__PURE__*/React.createElement("span", null, "Co-invented a ", /*#__PURE__*/React.createElement("em", null, "patented"), " real-time B2B payment system at JPMorgan.")), /*#__PURE__*/React.createElement("li", null, /*#__PURE__*/React.createElement("span", {
    className: "ach-num"
  }, "iii."), /*#__PURE__*/React.createElement("span", null, "Navigated a successful acquisition, preserving ", /*#__PURE__*/React.createElement("em", null, "millions in assets"), " during transition.")), /*#__PURE__*/React.createElement("li", null, /*#__PURE__*/React.createElement("span", {
    className: "ach-num"
  }, "iv."), /*#__PURE__*/React.createElement("span", null, "Built an enterprise-grade 50+ microservice platform from scratch as sole engineer in ", /*#__PURE__*/React.createElement("em", null, "six months.")))))));
}

// ─────────────────────────────────────────────────────────
// Projects
// ─────────────────────────────────────────────────────────
const PROJECTS = [{
  num: '04.i',
  name: 'Kash',
  role: 'COO & Interim CTO',
  period: '2025 –',
  body: 'COO and CTO from inception of an AI-powered prediction marketplace — users trade straight from social media, and the AI parses their intent, creates the markets and resolves them. Full ownership across product, technology, operations, finance and partnerships. Built the entire 50+ microservice platform as sole engineer in six months, then shipped the company around it — live in production, processing real payments.',
  pills: ['Operations', 'Product', 'Technology', 'AI', 'Finance', 'Partnerships'],
  url: 'https://kash.bot/'
}, {
  num: '04.ii',
  name: 'Voi Network',
  role: 'Chief Ecosystem Officer',
  period: '2024 – 2025',
  body: 'Led vision and development of an L1 from concept to $150M ecosystem. Built and mentored teams across departments. Integrated 25+ projects into the ecosystem. 45K+ users with strong token retention. Exited the role in 2025.',
  pills: ['Ecosystem Growth', 'Team Leadership', 'Strategic Vision'],
  url: 'https://voi.network/'
}, {
  num: '04.iii',
  name: 'Pound Token',
  role: 'Chief Technology Officer',
  period: '2023 – 2024',
  body: 'Led technical strategy through a successful acquisition. Executed zero-downtime migration reducing operating costs by 94%. Preserved millions in assets during the transition.',
  pills: ['Technical Strategy', 'Migration', 'Team Retention']
}, {
  num: '04.iv',
  name: 'Nettle',
  role: 'Chief Product & Technology Officer',
  period: '2022 – 2023',
  body: 'Led product and technology for an innovative fintech payments and loyalty platform. Architected a multi-currency payment system. Accelerated time-to-market through customer-driven development. The company later ceased operations.',
  pills: ['Product Strategy', 'Payment Systems', 'Team Building']
}];
function Projects() {
  return /*#__PURE__*/React.createElement("section", {
    id: "projects",
    className: "projects-section",
    "data-screen-label": "04 Projects"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    className: "num"
  }, "\xA7 04 \xB7 Featured projects"), /*#__PURE__*/React.createElement("h2", {
    className: "ttl"
  }, "Selected ", /*#__PURE__*/React.createElement("em", null, "work."))), /*#__PURE__*/React.createElement("div", {
    className: "right"
  }, "pp. 32\u201348")), /*#__PURE__*/React.createElement("div", {
    className: "list-divided"
  }, PROJECTS.map((p, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    className: "project"
  }, /*#__PURE__*/React.createElement("div", {
    className: "project-meta"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 ", p.num), /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, p.period)), /*#__PURE__*/React.createElement("div", {
    className: "project-body"
  }, /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, p.name, /*#__PURE__*/React.createElement("span", {
    className: "project-role"
  }, " \xB7 ", p.role)), /*#__PURE__*/React.createElement("p", null, p.body), p.url && /*#__PURE__*/React.createElement("p", {
    style: {
      marginTop: 16
    }
  }, /*#__PURE__*/React.createElement("a", {
    className: "link-arrow",
    href: p.url,
    target: "_blank",
    rel: "noreferrer"
  }, p.url.replace(/^https?:\/\//, '').replace(/\/$/, ''), " \u2192")), /*#__PURE__*/React.createElement("div", {
    className: "project-pills"
  }, p.pills.map((t, j) => /*#__PURE__*/React.createElement("span", {
    key: j,
    className: "pill"
  }, t)))))))));
}

// ─────────────────────────────────────────────────────────
// Services
// ─────────────────────────────────────────────────────────
function Services() {
  return /*#__PURE__*/React.createElement("section", {
    id: "services",
    className: "services-section",
    "data-screen-label": "05 Services"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    className: "num"
  }, "\xA7 05 \xB7 Services"), /*#__PURE__*/React.createElement("h2", {
    className: "ttl"
  }, "For founders, ", /*#__PURE__*/React.createElement("em", null, "operators"), " & boards.")), /*#__PURE__*/React.createElement("div", {
    className: "right"
  }, "Available via DD Ventures")), /*#__PURE__*/React.createElement("div", {
    className: "services-grid"
  }, /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 05.a"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Executive & ", /*#__PURE__*/React.createElement("em", null, "Operational"), /*#__PURE__*/React.createElement("br", null), "Advisory"), /*#__PURE__*/React.createElement("ul", {
    className: "bare-list"
  }, /*#__PURE__*/React.createElement("li", null, "Organisational design & scaling"), /*#__PURE__*/React.createElement("li", null, "Operational process architecture"), /*#__PURE__*/React.createElement("li", null, "Financial controls & governance"), /*#__PURE__*/React.createElement("li", null, "Executive leadership coaching"))), /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 05.b"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Product & ", /*#__PURE__*/React.createElement("em", null, "Technology"), /*#__PURE__*/React.createElement("br", null), "Strategy"), /*#__PURE__*/React.createElement("ul", {
    className: "bare-list"
  }, /*#__PURE__*/React.createElement("li", null, "Product-market fit analysis"), /*#__PURE__*/React.createElement("li", null, "Technical architecture review"), /*#__PURE__*/React.createElement("li", null, "AI, blockchain & payments strategy"), /*#__PURE__*/React.createElement("li", null, "Scalability & infrastructure planning"))), /*#__PURE__*/React.createElement("article", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 05.c"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "Growth & ", /*#__PURE__*/React.createElement("em", null, "Go-to-Market")), /*#__PURE__*/React.createElement("ul", {
    className: "bare-list"
  }, /*#__PURE__*/React.createElement("li", null, "Ecosystem & community growth"), /*#__PURE__*/React.createElement("li", null, "Partnership & BD strategy"), /*#__PURE__*/React.createElement("li", null, "Stakeholder & investor management"), /*#__PURE__*/React.createElement("li", null, "Team building & culture")))), /*#__PURE__*/React.createElement("div", {
    className: "dd-callout"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "\xA7 05 \xB7 A note"), /*#__PURE__*/React.createElement("h3", {
    className: "subhead"
  }, "DD Ventures \u2014 ", /*#__PURE__*/React.createElement("em", null, "entrepreneurship"), " advisory"), /*#__PURE__*/React.createElement("p", null, "Specialised advisory leveraging deep entrepreneurship experience to help founders successfully launch, run and scale companies and products."), /*#__PURE__*/React.createElement("a", {
    className: "link-arrow",
    href: "https://dd.ventures",
    target: "_blank",
    rel: "noreferrer"
  }, "dd.ventures \u2192"))));
}

// ─────────────────────────────────────────────────────────
// Writing & Speaking
// ─────────────────────────────────────────────────────────
const ARTICLES = [{
  date: 'May 2026',
  title: 'Building a Second Brain That Doesn\'t Die',
  excerpt: 'The opinionated PKM template, open-sourced under MIT. Populated by a Claude Code setup wizard in 45 minutes.',
  url: '/writing/building-a-second-brain/'
}, {
  date: 'Feb 2026',
  title: 'In Clawd We Trust',
  excerpt: 'A year of intensive Claude Code, distilled. The mental model, environment, phased workflow and quality practices that turn it into a 100x force multiplier.',
  url: '/writing/in-clawd-we-trust/'
}, {
  date: 'Jul 2025',
  title: 'Prediction Markets',
  excerpt: 'From gambling to humanity\'s decision engine. Why prediction markets are the most important information primitive nobody\'s using.',
  url: '/writing/prediction-markets/'
}, {
  date: 'Feb 2025',
  title: 'Tesserakt Protocol',
  excerpt: 'Purpose-built infrastructure for AI agents. The missing protocol layer between models and the world.',
  url: '/writing/tesserakt-protocol/'
}];
const SPEAKING = [{
  event: 'Zebu Live',
  topic: 'Tokenomics Design &amp; Strategy'
}, {
  event: 'Algorand at Tate Modern',
  topic: 'Tokenomics Design &amp; Strategy'
}];
function Writing() {
  return /*#__PURE__*/React.createElement("section", {
    id: "writing",
    className: "writing-section",
    "data-screen-label": "06 Writing"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    className: "num"
  }, "\xA7 06 \xB7 Writing & speaking"), /*#__PURE__*/React.createElement("h2", {
    className: "ttl"
  }, "In ", /*#__PURE__*/React.createElement("em", null, "print"), " and at the lectern.")), /*#__PURE__*/React.createElement("div", {
    className: "right"
  }, "Latest four \xB7 technology")), /*#__PURE__*/React.createElement("div", {
    className: "writing-grid"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "kicker",
    style: {
      marginBottom: 24
    }
  }, "Recent articles"), /*#__PURE__*/React.createElement("div", {
    className: "list-divided"
  }, ARTICLES.map((a, i) => /*#__PURE__*/React.createElement("a", {
    key: i,
    className: "article-link",
    href: a.url
  }, /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, a.date), /*#__PURE__*/React.createElement("h3", {
    className: "subhead",
    dangerouslySetInnerHTML: {
      __html: a.title
    }
  }), /*#__PURE__*/React.createElement("p", {
    dangerouslySetInnerHTML: {
      __html: a.excerpt
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "link-arrow"
  }, "Read \u2192")))), /*#__PURE__*/React.createElement("p", {
    style: {
      marginTop: 32
    }
  }, /*#__PURE__*/React.createElement("a", {
    className: "link-arrow",
    href: "/writing/"
  }, "All writing \u2192"))), /*#__PURE__*/React.createElement("aside", {
    className: "speaking"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker",
    style: {
      marginBottom: 24
    }
  }, "Conference speaking"), SPEAKING.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "speaking-item"
  }, /*#__PURE__*/React.createElement("h4", {
    className: "subhead",
    style: {
      fontSize: '24px'
    }
  }, s.event), /*#__PURE__*/React.createElement("div", {
    className: "meta",
    dangerouslySetInnerHTML: {
      __html: s.topic
    }
  }))), /*#__PURE__*/React.createElement("hr", {
    className: "hairline",
    style: {
      margin: '40px 0'
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "kicker",
    style: {
      marginBottom: 16
    }
  }, "Open to \u2014"), /*#__PURE__*/React.createElement("p", {
    style: {
      fontFamily: 'var(--font-serif)',
      fontStyle: 'italic',
      fontSize: '20px'
    }
  }, "Keynotes & panels on fintech, blockchain, AI, distributed systems and the operator's lens on technical leadership.")))));
}

// ─────────────────────────────────────────────────────────
// Contact
// ─────────────────────────────────────────────────────────
function Contact() {
  return /*#__PURE__*/React.createElement("section", {
    id: "contact",
    className: "contact-section",
    "data-screen-label": "08 Contact"
  }, /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    className: "num"
  }, "\xA7 08 \xB7 Get in touch"), /*#__PURE__*/React.createElement("h2", {
    className: "ttl"
  }, "Operations, ", /*#__PURE__*/React.createElement("em", null, "strategy,"), /*#__PURE__*/React.createElement("br", null), "architecture.")), /*#__PURE__*/React.createElement("div", {
    className: "right"
  }, "Available now")), /*#__PURE__*/React.createElement("div", {
    className: "contact-grid"
  }, /*#__PURE__*/React.createElement("p", {
    className: "lede",
    style: {
      maxWidth: 720
    }
  }, "Open to conversations with founders and operators building real things across fintech, blockchain and AI. Operating roles direct; advisory, fractional and board engagements through DD Ventures."), /*#__PURE__*/React.createElement("div", {
    className: "contact-links"
  }, /*#__PURE__*/React.createElement("a", {
    href: "mailto:me@benedictdixon.com",
    rel: "me"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "Email"), /*#__PURE__*/React.createElement("div", {
    className: "contact-value"
  }, "me@benedictdixon.com")), /*#__PURE__*/React.createElement("a", {
    href: "https://www.linkedin.com/in/benedict-dixon/",
    target: "_blank",
    rel: "me noreferrer"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "LinkedIn"), /*#__PURE__*/React.createElement("div", {
    className: "contact-value"
  }, "/in/benedict-dixon")), /*#__PURE__*/React.createElement("a", {
    href: "https://x.com/bwhdx",
    target: "_blank",
    rel: "me noreferrer"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "Twitter"), /*#__PURE__*/React.createElement("div", {
    className: "contact-value"
  }, "@bwhdx")), /*#__PURE__*/React.createElement("a", {
    href: "https://github.com/bwhdx",
    target: "_blank",
    rel: "me noreferrer"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "GitHub"), /*#__PURE__*/React.createElement("div", {
    className: "contact-value"
  }, "@bwhdx")), /*#__PURE__*/React.createElement("a", {
    href: "https://dd.ventures",
    target: "_blank",
    rel: "noreferrer"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kicker"
  }, "Advisory"), /*#__PURE__*/React.createElement("div", {
    className: "contact-value"
  }, "dd.ventures"))))), /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("footer", {
    className: "colophon"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("strong", null, "Colophon"), "Set in Fraunces, Inter & JetBrains Mono. Hero portrait rendered from a brightness sample of a photograph, redrawn in candlesticks. Royal navy on warm cream."), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("strong", null, "Edition"), "Vol. I \xB7 MMXXVI", /*#__PURE__*/React.createElement("br", null), "An Almanack for Finance, Operations & Code"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("strong", null, "Words"), "Approx. 1,400.", /*#__PURE__*/React.createElement("br", null), "Reading time: ~6 minutes."), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("strong", null, "\xA9 2026"), "Benedict W. H. Dixon.", /*#__PURE__*/React.createElement("br", null), "All rights observed."))));
}
Object.assign(window, {
  Runner,
  Hero,
  Ticker,
  About,
  Projects,
  Services,
  Writing,
  Contact
});

/* ── portfolio-app.js ────────────────────────────────────────── */
// Top-level composition of the portfolio page.

function App() {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("a", {
    className: "skip-link",
    href: "#main-content"
  }, "Skip to main content"), /*#__PURE__*/React.createElement(Runner, null), /*#__PURE__*/React.createElement("main", {
    id: "main-content"
  }, /*#__PURE__*/React.createElement(Hero, null), /*#__PURE__*/React.createElement(Ticker, null), /*#__PURE__*/React.createElement(About, null), /*#__PURE__*/React.createElement("div", {
    id: "morph"
  }, /*#__PURE__*/React.createElement(MorphStage, null)), /*#__PURE__*/React.createElement(Ticker, null), /*#__PURE__*/React.createElement(Projects, null), /*#__PURE__*/React.createElement(Services, null), /*#__PURE__*/React.createElement(Writing, null), /*#__PURE__*/React.createElement(Contact, null)), /*#__PURE__*/React.createElement(CursorTrail, null));
}
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));